/**
 * TypeScript/PropTypes type definitions
 * For TypeScript projects, use .ts extension and proper types
 * For JavaScript projects, use PropTypes or JSDoc comments
 */

// Example: User type
export const UserType = {
  id: String,
  name: String,
  email: String,
};

// Example: Navigation types
export const NavigationTypes = {
  // Define your navigation param types here
};

